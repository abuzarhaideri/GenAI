# Primary (no key required): Pollinations text endpoint
# This project will use Pollinations by default even if you set no keys.

# Optional fallback (single key): OpenRouter
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Optional: choose a model on OpenRouter
OPENROUTER_MODEL=openai/gpt-4o-mini
